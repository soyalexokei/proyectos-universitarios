/* 1.2. Program about making an integer variable and printing its value on the screen (printf function). */
#include <stdio.h>

int main (void)
{
    int num = 2;
    printf("\nMy favourite number is = %d\n", num);
    return 0;
}
