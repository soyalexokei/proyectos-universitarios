/* 1.3. Program about making 3 integer variables,
adding the values of the first two,
and storing the result in the third one.
Print the result.*/
#include <stdio.h>

int main (void)
{
    int A = 10;
    int B = 35;
    int sol = A + B;
    printf("\nThe addition between A and B is = %d\n", sol);
    return 0;
}
